#include <iostream>
#include <queue>
#include <vector>

#define MAX 10

using namespace std;

int MAP[MAX][MAX];
bool visited[MAX][MAX];

int T;
int N;

void Input();
void BFS(int x, int y);
void IsQueen();

int main(void)
{
    
    return 0;
}

void Input(){
    cin >> T;
}

void IsQueen(){
    for(int i = 0; i < N; i++){
        
    }
}

void BFS(int x, int y){
    int nx, ny;
    queue<pair<int, int> > q;
    q.push(make_pair(x, y));
    
    while(!q.empty()){
        for    
    }
}
